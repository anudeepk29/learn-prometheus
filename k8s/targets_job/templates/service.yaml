apiVersion: v1
kind: Service
metadata:
  name: {{ include "targets_job.fullname" . }}
  labels:
    {{- include "targets_job.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "targets_job.selectorLabels" . | nindent 4 }}
