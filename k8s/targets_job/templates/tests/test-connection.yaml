apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "targets_job.fullname" . }}-test-connection"
  labels:
    {{- include "targets_job.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "targets_job.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
